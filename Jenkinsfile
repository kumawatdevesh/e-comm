pipeline {
    agent any 

    stages {
        stage("build") {
            echo "building the applictaion..."
        }

        stage("test") {
            echo "testing the application..."
        }

        stage("deploy") {
            echo "deploying the application..."
        }
    }
}
